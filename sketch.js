//DOM
//by Eddson Jose
//------------------------------------------------------------------
function setup() {
  createCanvas(windowWidth, windowHeight);
  frameRate(10);
}
//------------------------------------------------------------------
function draw() {
  background(50);
  noStroke();
  fill(255);
  circle(width/2, height/2, 30);
}
//------------------------------------------------------------------
